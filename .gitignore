# 환경 설정 (API 키 포함)
.env

# 벡터DB (각자 로컬에서 ingest 스크립트로 생성)
chroma_db/

# PDF 원본 매뉴얼 (용량 크므로 별도 공유 - README 참고)
data/lg/
data/samsung/

# 그림 추출 결과 (ingest 시 자동 생성)
data/manual_figures/

# 에러코드 SQLite DB (seed_error_codes.py로 재생성)
data/error_codes.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.conda/

# 실험 결과는 커밋한다 (GitHub Actions가 자동 보고서 생성에 사용)
# experiments/results/ → gitignore 제외

# Jupyter
.ipynb_checkpoints/
notebooks/.ipynb_checkpoints/

# Claude Code 로컬 설정
.claude/

# OS
.DS_Store
Thumbs.db

# data 폴더 제외
/data

# chroma db
/chroma_lg_aircon
# siyeon: 생성물 (build_db.py / 노트북 영속화)
data/appliance.sqlite
chroma_lg_aircon/
chroma_lg/
data/appliance_new.sqlite
data/answer_cache/

# 웹 앱 DB (사용자·대화 — 로컬 데이터)
data/app.sqlite

# 썸네일 검수용 모음 (재생성 가능)
web/static/img/models/auto/sheet.png

# siyeon: 커밋 안 하는 것 — 썸네일 원본(avif, 웹은 png 사용) / 옛 예상질문 캐시 v1 / 선화 썸네일(make_thumbs.py로 재생성, LG는 실사진 있음)
experiments/siyeon/LG_images/
experiments/siyeon/chunk_questions_v1.json
web/static/img/models/auto/
