# Claude 지침서 — ask-my-appliance

AI 어시스턴트에게 작업을 맡길 때 이 파일을 먼저 읽힌다.

---

## 프로젝트 한 줄 요약

LG·삼성 가전 설명서 60개를 근거로만 답하는 RAG 챗봇. **실험 단계는 끝났고 통합된 서비스가 있다.**
문제 정의부터 평가까지는 [`docs/`](docs/) 참고.

## 구조

| 경로 | 성격 | 수정 시 주의 |
|---|---|---|
| `web/` | **제품 코드** | 실사용에 영향. 바꾸면 실제로 돌려보고 확인 |
| `scripts/`, `supabase/` | 제품 운영 | 공유 DB·크롤러 |
| `docs/` | 프로젝트 문서 | 수치를 바꿀 땐 결과 파일에서 확인하고 쓴다 |
| `experiments/` | 실험 기록 | 과거 기록이라 함부로 고치지 않는다 |

## 절대 건드리지 않는 것

| 대상 | 이유 |
|---|---|
| `experiments/harness/evaluate.py`의 `MODEL_QUESTIONS`, `SYSTEM_PROMPT` | 팀 비교 기준. 바꾸면 과거 결과와 비교 불가 |
| `.env` | 비밀 값. 내용을 출력하거나 채팅에 붙이지 않는다 |
| `data/`, `chroma_db/` | 용량·저작권 문제로 Git 제외 |

## 작업 원칙

**1. LLM 판단을 신뢰해야만 성립하는 기능은 코드로 바꾼다.**
이 프로젝트에서 반복 확인된 패턴이다. 안전 경고, 그림 선택, 쪽 번호는 전부 프롬프트로 지시했다가
실패해서 결정론적 코드로 옮겼다. 새 기능도 같은 기준으로 판단한다.

**2. 추상적 지시보다 구체적 예시.**
프롬프트를 고칠 때 "필요한 만큼 쓰세요" 같은 추상적 지시는 통하지 않았다. 나쁜 예/좋은 예를 직접
넣어야 동작했다. 그것도 안전 관련 이진 판단에는 충분하지 않았다.

**3. 한 사례로 전체를 바꾸지 않는다.**
실사용 로그 1건으로 파이프라인을 바꾸기 전에 300문항 등 기존 평가로 회귀를 확인한다.
청킹 오버랩 실험 3종이 전부 실패했던 것처럼, 좋아 보이는 변경이 실제로는 나쁜 경우가 많다.

**4. 측정하고 나서 말한다.**
"개선됐다"고 쓰기 전에 수치를 낸다. 눈 검수면 표본 크기를 함께 적는다.

## 비용

OpenAI 호출은 전부 과금된다. **300문항 평가 등 대량 실행 전에는 사용자에게 먼저 확인한다.**
(질문당 리랭킹·생성·판정 3회 → 전체 약 900회)

## git

- 커밋은 사용자 확인 후에 한다. `main`은 보호 브랜치라 원칙상 PR을 거친다.
- 팀 공용 파일(`experiments/harness/`, `experiments/siyeon/`)을 고치면 팀에 공유해야 한다고 알린다.
- `.env`, `data/`, 큰 바이너리가 스테이징에 섞이지 않았는지 커밋 전에 확인한다.

## 변수명 규칙

| 역할 | 이름 |
|---|---|
| 사용자 질문 | `query` |
| 검색 결과 목록 | `candidates` |
| 검색 결과 개수 | `top_k` |
| 청크 고유 ID | `chunk_id` |
| 유사도 거리 (낮을수록 좋음) | `distance` |
| 임베딩 벡터 | `embedding` |
| 매뉴얼 식별자 | `manual_id` / `doc_id` |

## RAG를 바꿀 때

`web/rag_service.py`의 함수 4개(`understand_query` / `retrieve` / `stream_answer` / `sources_of`)가
교체 지점이다. 이 입출력만 지키면 라우터·프런트·DB는 그대로 둘 수 있다.
자세한 계약은 [`web/README.md`](web/README.md).
